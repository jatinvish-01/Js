
// Importants thing for interview.

// 1. Primitive Datatypes:-

//there arprimitie 7 types of ve datatypes:- String, Number, Boolearn, Null, Undefined, Objects, Symbol, BigInt. 

// Examples:

const score = 100
const scoreValue = 100.90

const isLoggedIn = false
const outIdeTemp = null
let userName;

// symbol-> used for unique idenfication of the variable
const id = Symbol('jatin')
const anotherId = Symbol('1234')

console.log(id === anotherId); // Output-- false because they are not same var.



// 2. Reference (Non primitive)

/*Array, Objects, Functions*/

// Examples--------->

//Arrays
const heros = ["shaktiman", "spiderman", "antman"]

// Objects
let myobj = {
    name: "jatin",
    age: 23,        //datatype kuch bhi ho skta hai
    item: color
}

// Functions
const myFunction = function(){
    console.log("Hello World!")
}

console.log(typeof anotherId);

// [Master JavaScript]:--(Objects, Broswer events)

// Ques 1: Is Javascript statically or dynamically types?
// Ans: JavaScript is dynamically typed, which means that the interpreter assigns a variable's type at runtime based on the value of the variable
